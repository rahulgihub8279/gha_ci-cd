name: greeting
on: 
  push:
    branches: 
      - main
      - "release/**"  
    paths-ignore: 
      - '*.md'
      - '.gitignore'
      - '/github/workflows/*'
  pull_request: 
    branches: 
      - main 
  workflow_dispatch: 
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build step
        run: echo "build..."
      - name: final step
        run: echo "finalize..."  
