name: greeting

on: workflow_dispatch

jobs:
   build:
     runs-on: ubuntu-latest
     steps:
       - name: Build step
         run: echo "build..."
   test:
      runs-on: ubuntu-latest
      needs: build
      steps:
        - name: test step
          run: echo "test..."
   deploy:
     runs-on: ubuntu-latest
     needs: test
     steps:
       - name: deploy
         run: echo "deploy..."
        
        
  
