name: greeting

on: workflow_dispatch

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: greet user
        run: |
          echo "welcome user..." 
  second_job:
    runs-on: ubuntu-latest
    steps:
      - name: update system
        run: sudo apt update
  third_job:
    runs-on: ubuntu-latest
    steps:
      - name: prepare_build
        run: echo "setting up..."
        
        
  
